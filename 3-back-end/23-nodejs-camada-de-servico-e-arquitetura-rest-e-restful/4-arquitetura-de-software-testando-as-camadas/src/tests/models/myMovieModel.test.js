const { expect } = require('chai');

describe('Ao resgatar um filme do banco de dados', () => {
  describe('Quando o filme existe', () => {
    it('Retorna os dados do filme', () => {});
  });

  describe('Quando o filme não existir', () => {
    it('Retorna uma mensagem de erro', () => {});
  });
});
